package com.example.androidarchitecture.data.model

enum class Status {
    SUCCESS,
    LOADING,
    ERROR,
    SIGN_UP,
    SIGN_IN,
    INVALID_CREDENTIALS,
    INVALID_ACCESS_TOKEN,
    INVALID_USER
}